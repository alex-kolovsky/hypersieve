[package]
name = "hypersieve"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
lazy_static = { version = "1.5.0", features = ["spin_no_std"]}
spin = "0.12.2"
